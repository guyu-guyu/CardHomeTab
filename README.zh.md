# CardHomeTab

**中文** · [English](README.md)

把一篇 Markdown 笔记变成启动页。CardHomeTab 按标题切分这篇笔记，用 Obsidian 自己的渲染器把每个 section 渲染成一张卡片，并允许每张卡片引用自己的 CSS。

## 功能

- 首页视图：logo（内置图标、仓库图片或网络图片）、文字标识、背景图（亮/暗两套，支持模糊与压暗）。
- 全库文件名模糊搜索，并支持书签与最近文件。`Enter` 在当前标签打开，`Ctrl`/`Cmd`+`Enter` 在新标签打开。
- 卡片内容取自单个仪表盘笔记，按可配置的标题级别切分。
- 大部分内容类型都能渲染，因为渲染走的是 Obsidian 的 `MarkdownRenderer`：纯文本、代码块、```` ```base ```` 块，以及装上 dataview 后的 ```` ```dataview ```` / ```` ```dataviewjs ```` 块。核心 ```` ```query ```` 嵌入搜索块除外——它在卡片里会以普通代码块形式呈现查询源码，而非搜索结果（见已知限制）。
- 卡片内容落在一篇真实笔记里，因此 `![[某表.base]]`、`![[图片.png]]` 与相对链接都按仪表盘笔记所在目录解析。
- 每张卡片的元数据写在 Obsidian 原生注释 `%%card: %%` 里：CSS 片段、跨列数与图标。
- 设置里提供按内容类型的全局样式开关，本期为表格斑马条纹。
- 支持引用 `.obsidian/snippets/` 下的自有片段，按卡片生效。
- 拖拽排序卡片；卡片正文用普通笔记编辑器修改。
- 移动端与桌面端都能渲染卡片，且只使用公开 API——但卡片操作条是悬停交互，触屏上够不到设置、编辑、删除与拖拽（见已知限制）。
- 在标签页里编辑仪表盘笔记时，首页卡片同步更新。

## 安装

### 社区插件

在 Obsidian 里打开 **设置 → 第三方插件 → 浏览**，搜索 "CardHomeTab" 安装并启用。该方式需要插件已被收录进社区目录。

### 手动安装

1. 从最新 release 下载 `main.js`、`manifest.json`、`styles.css`；或在仓库根目录执行 `npm ci && npm run build`，从 `dist/` 取这三个文件。
2. 在库里创建 `<你的库>/.obsidian/plugins/card-home-tab/`。
3. 把三个文件复制进去。
4. 重载 Obsidian，在 **设置 → 第三方插件** 里启用 "CardHomeTab"。

`dist/` 是自包含的构建产物：每次构建都会清空并重新生成，可以直接安装或发布。

## 仪表盘笔记

默认仪表盘文件为 `Home.md`。标题级别等于**卡片标题级别**（默认 2 级）的标题切出一张卡片，卡片正文延伸到下一个同级或更浅的标题。

文件不存在时：**打开新标签页**会自动按默认内容创建它——两张宽卡片（`span=2` 铺满整行），各是一个内联 base 块：最近修改（`file.mtime` 倒序）与最近创建（`file.ctime` 倒序），各取 10 条，并把仪表盘自身排除在外。**手动打开首页视图**则仍停在「没有找到仪表盘文件」的提示页上，由你点「创建并打开」——那条路径上常见情形是设置里的路径填错了，静默建文件反而更糟。

第二张卡原本想要的是「添加了书签的文件」，但做不到：Bases 既没有书签属性，函数表里也没有书签相关函数，书签存在 `.obsidian/bookmarks.json` 里、Bases 读不到。

```markdown
# 我的首页

## 统计
%%card: css=stats; span=2; icon=lucide-chart%%
- 库内文件：`1204` 个

## 待办
%%card: css=user:todo%%
- [ ] 复核发布说明

### 这个三级标题留在卡片内部
## 这里开始另一张卡片
```

- 比卡片标题级别更浅的标题（例如 H1 大标题）始终是页面级的：不会生成卡片，且会终止它上面的那张卡片；YAML frontmatter 同样被忽略。
- 比卡片标题级别更深的标题留在卡片内部正常渲染。
- 标题文字即卡片标题。
- 没有 `%%card:` 行的 section 就是一张纯内容卡片：`span=1`、无片段、无图标。
- 仪表盘就是一篇普通笔记，可以正常打开、编辑、搜索、同步与做版本对比。

### `%%card:` 注释

元数据行必须位于标题之后第一个非空行。

```markdown
%%card: css=mine; span=2; icon=lucide-chart%%
```

| 键 | 取值 | 含义 |
| --- | --- | --- |
| `css` | 逗号分隔的片段引用列表 | 只作用于这张卡片的 CSS 片段 |
| `span` | 大于等于 1 的整数 | 卡片跨越的网格列数 |
| `col` | 大于等于 1 的整数 | 卡片所在列；不写则按笔记顺序轮转分配 |
| `icon` | Obsidian 图标 id，例如 `lucide-chart` | 显示在卡片标题前的图标 |

语法规则：

- 字段间用 `;` 分隔，多值用 `,` 分隔，键值用 `=`。
- 片段引用指向片段目录下的一个文件。裸名与 `user:` 前缀等价：`css=mine` 就是 `css=user:mine`。
- 解析不出来的引用会被忽略，这一行的其余部分照常生效。
- `col` 超出当前列数时会被夹到最后一列；`col` 与 `span` 一起越界时会退到放得下的位置，不会撑出多余的列。
- 未识别的键会原样保留。
- `%% %%` 是 Obsidian 原生注释语法：阅读模式下自动隐藏，Live Preview 下以暗色文字显示，这是 Obsidian 的原生行为，而不是插件引入了额外标记。
- 这一行由卡片设置弹窗生成，也完全可以手写。

## 卡片布局

卡片以**列**为单位排布：每张卡片归属某一列，列内自上而下依次排列、向上贴紧，列与列互不影响。这样拖动一张卡片时其余卡片不会跟着重排。

- 列归属写在 `%%card:` 的 `col` 里。没写 `col` 的卡片按笔记顺序轮转分配（第 1 张→列 1、第 2 张→列 2……）。
- **第一次拖动卡片时，插件会把所有还没写 `col` 的卡片补上它们此刻的实际列号。** 这是一次性的，写入的就是当时的显示结果，所以布局不会变；不这么做的话，轮转回退依赖笔记序号，而移动会让后续卡片的序号集体位移、连带跳列。
- 列内顺序由笔记顺序决定，所以拖动同时会改写笔记里 section 的先后。
- `span` 大于 1 的宽卡片会跨列。它落在所跨各列中最低的可用位置，并把这几列一起推到它下方——宽卡片会把它跨的列在该处耦合起来，这是跨列唯一合理的语义。判断落点时，宽卡片算作它**覆盖的每一列**的成员，所以顶部是宽卡片时也能把别的卡片拖到它上方。
- 卡片区默认撑满整个首页宽度。「页面 → 限制栏宽」可以给它设一个上限（所有列加上列间距的总宽）并居中，品牌区、搜索框与背景仍是满宽。
- 可用宽度窄于 900px 时收成单列，此时 `col` 被忽略。这条看的是**面板给出的宽度**，不是限制后的栏宽——把栏宽设成小于 900 不会让多列塌成一列。

## 内容样式

这类样式在设置里配一次、对所有卡片生效，常见需求因此不必写任何 CSS。各项彼此独立、可任意组合，默认值一律等于插件原本的样子，所以升级不会改变观感。规则随插件的 `styles.css` 发布，由首页根节点上的类或 CSS 变量控制，改回默认后不留痕迹。

设置页里按类型分成折叠块，展开后是该类型下的各项设置。每个折叠块（包括「品牌区」「背景」「搜索」）的标题右侧都有一个「重置」按钮，仅在该块有改动时出现，点它把这一块恢复为默认值。

| 类型 | 设置 | 作用 |
| --- | --- | --- |
| 卡片 | 显示卡片标题 | 关掉后标题与图标都不显示，卡片内容顶到最上面。拖拽手柄与设置、编辑、删除按钮会浮到卡片右上角，悬停时出现——它们和标题在同一层 DOM 里，所以是移开而不是一起隐藏。 |
| 卡片 | 圆角半径 | `0–24`，默认 8（与 Obsidian 的 `--radius-m` 一致）。 |
| 卡片 | 卡片间距 | `0–48`，默认 16。横向与竖向同时生效。 |
| 卡片 | 边框样式 | 无 / 实线 / 虚线 / 点线，默认实线。选「无」等同于把粗细调到 0。 |
| 卡片 | 边框粗细 | `0–8`，默认 1。 |
| 卡片 | 投影 | 不显示 / 一直显示 / 鼠标悬停时显示，默认不显示。强度取自主题的 `--shadow-s`。 |
| 表格 | 斑马条纹 | 让 Markdown 表格的数据行深浅背景交替。表头不受影响，悬停反馈保留，base 与 dataview 渲染出来的表格不参与。 |
| Base | 长条 base 表格 | 把表格视图的每一行画成一块整宽圆角色块，行间留出间距、去掉行列分隔线。单元格仍横向并排在一行内：Bases 表格是虚拟滚动，行与单元格的位置由 JS 算出，所以这个选项只做绘制、不改布局。 |
| Base | 隐藏顶部工具栏 | 隐藏顶部那一行控件（视图选择、结果数、排序、筛选、属性），只留内容。 |

颜色都可以用 CSS 变量覆盖（可按主题或按库设置）：斑马条纹是 `--home-tab-table-zebra`（默认取 `--background-modifier-hover`）、长条 base 表格是 `--home-tab-base-bar`、卡片投影是 `--home-tab-card-shadow`。

后续的类型（callout 等）会沿用同一套做法。

## 每卡 CSS 片段如何生效

- 引用写成裸文件名或 `user:文件名`，两者都读 `<配置目录>/snippets/文件名.css`——与 Obsidian 自身的 CSS 片段设置是同一个目录。
- **不要**在 **设置 → 外观 → CSS 片段** 里启用卡片片段：在那里启用会让它全局生效。插件自己读取文件内容，完全不碰 `enabledCssSnippets`。
- 片段原文会被整段包进

  ```css
  @scope (.home-card.home-card[data-card-id="…"]) {
    /* 片段原文 */
  }
  ```

  再作为 constructable stylesheet 挂到文档上，因此其中的规则只可能匹配这一张卡片。作用域根类名重复了一次，把优先级抬到 `0,2,0`，避免主题选择器盖过你的片段。
- `:root` 会被改写成 `:scope`。在 `@scope` 内 `:root` 仍指向文档根，永远不会匹配。`.theme-dark .foo` 这类选择器不需要特殊处理：主题类挂在 `body` 上，天然仍然可匹配。
- 片段里不允许使用 `@import`。这样的片段会被拒绝并打日志，卡片就不带它渲染；同一卡片上的其他片段不受影响。
- 样式匹配的是渲染出来的 DOM。作用域内的结构如下：

  ```text
  .home-card[data-card-id]
  ├── .home-card-header        图标、标题、悬停操作条
  └── .home-card-content.markdown-rendered
      └── .home-card-content-inner
          └── 渲染出的 Markdown
  ```

  `markdown-rendered` 加在 `.home-card-content` 自身上，所以能匹配的写法是 `.home-card-content.markdown-rendered p { … }`。

## 使用首页

鼠标悬停在卡片上会显示操作条：

| 操作 | 行为 |
| --- | --- |
| 拖拽手柄 | 把卡片拖到某一列的某个位置。拖动时会在落点显示一条横线，紧贴它将插到哪张卡片的下方 |
| 卡片设置 | 弹窗设置图标、CSS 片段、跨列数与所在列；写回 `%%card:%%` 行并只重渲染该卡 |
| 编辑 | 打开仪表盘笔记并把光标定位到该卡片的标题行 |
| 删除 | 二次确认后移除该 section |

首页上的卡片是只读渲染的，所有写入都发生在笔记里。

命令：

| 命令 | 含义 |
| --- | --- |
| `打开首页` | 打开首页视图 |
| `刷新所有卡片` | 重新渲染全部卡片 |
| `在标签页打开仪表盘` | 在标签页里打开仪表盘笔记 |
| `新建卡片` | 在仪表盘笔记末尾追加一个卡片 section |

## 设置项

| 分区 | 设置 |
| --- | --- |
| 页面 | 仪表盘文件（带笔记补全的文本输入框，因此可以填一个还不存在的路径）、卡片标题级别（H2–H6）、网格列数（1–6，默认 2）、限制栏宽与栏宽宽度（600–2400，仅在开启限制时显示）、替换新标签页、启动时打开首页 |
| 品牌区 | Logo 类型（无 / 内置图标 / 仓库图片 / 网络图片）、图标 id 或图片路径、Logo 缩放（0.2–5）、Logo 颜色（留空跟随主题强调色，仅对内置图标生效）、文字标识文案、显示文字标识、文字标识字号、文字标识字重（100–900） |
| 背景 | 背景类型（无 / 仓库图片 / 网络图片）、亮色背景、暗色背景（留空则暗色模式也用亮色）、背景模糊（0–40）、背景压暗（0–100） |
| 搜索 | 显示搜索框、仅搜索 Markdown、显示路径、显示书签、显示最近文件、结果数（1–50）、最近文件条数（0–20） |
| 内容样式 | 按类型分组的折叠块：卡片（显示标题、圆角、间距、边框样式与粗细、投影）、表格（斑马条纹）、Base（长条 base 表格、隐藏顶部工具栏） |
| 片段 | 自有片段及其路径、片段目录、「刷新列表」按钮 |

## 已知限制

1. **只支持一个仪表盘文件。** 没有多套仪表盘页面切换。
2. **卡片正文里不能出现同级标题**——与卡片标题级别同级的标题会被切成另一张卡片。更深级别的标题不受影响。
3. **换行符可能混用。** `updateCardMeta` 与 `appendCard` 一律插入 `\n`，因此编辑一篇 CRLF 换行的仪表盘笔记时，被改动的那一行会变成 `\n`，而文件其余部分仍是 `\r\n`。解析不受影响，只是文件在插件写过的那一行上不再是统一的 CRLF。
4. **设置项不会被 Obsidian 1.13+ 的设置搜索索引。** 设置页是命令式的（`display()`），这是 `minAppVersion` 1.9.0 的要求，且 `getSettingDefinitions()` 返回空数组；1.13+ 会正常显示该页面，但搜不到里面的设置项。
5. **`%%card:` 的值里不能含 `;`**（字段分隔符），没有 `=` 的片段也无法表示。遇到这类无法表示的内容，插件会拒绝改写该行并提示你手工调整，而不是静默丢弃。
6. **CSS 片段不能使用 `@import`。** 这样的片段会被拒绝并打日志，卡片就不带它渲染。
7. **没有内置片段。** 插件不再自带任何 CSS 片段：反复出现的需求由设置里的「内容样式」覆盖，其余交给你自己的片段。存量的 `css=auto` 会被当成一个名为 `auto` 的片段去读，存量的 `css=builtin:x` 则完全解析不出来——两者都只是被忽略。
8. **需要 Obsidian 1.9.0 及以上。** `@scope` 需要 Chromium 118+，而 `base` 块是 1.9.0 引入的，因此 `minAppVersion` 为 1.9.0。全部功能都只用公开 API，所以移动端也能加载插件并渲染卡片。
9. **`span` 与列数不整除时最后一列会空置。** 例如 3 列网格里所有卡片都是 `span=2`，它们只能挨个占住列 1-2，第 3 列谁也占不到。这是"跨列"本身的约束（2 在 3 列里放不下第二个），不是布局的缺陷；这种排版把网格列数设成 2 更合适。
10. **内容样式只管 Markdown 内容。** 表格斑马条纹有意跳过 base 视图与 dataview 渲染出来的表格：它们自带行结构与主题样式，插手只会互相打架。
11. **非目标。** 卡片不能折叠、不能嵌套卡片、不能内联编辑，也不能每张卡单独存一个文件——卡片就是仪表盘笔记里的一个 section。`%%card: %%` 这一行在 Live Preview 下是可见的暗色文字。
12. **卡片操作条只靠悬停显示。** 它由卡片上的 `:hover`/`:focus-within` 触发，其中的控件是不可聚焦的 `span`。触屏没有悬停，键盘也没有路径够到它们，因此设置、编辑、删除与拖拽目前只在桌面端可用；卡片本身在移动端正常渲染。
13. **仪表盘文件必须是 Markdown 笔记。** 路径不以 `.md` 结尾时会被拒绝：首页显示为文件缺失，创建时弹出提示，因此不能把插件指到一个 `.canvas` 之类的文件上。
14. **有两个命令会在文件缺失时创建仪表盘文件。** `新建卡片` 与 `在标签页打开仪表盘` 会按设置里当前那个路径直接创建该笔记，不会再问一次；`新建卡片` 随后往里追加一张卡片。路径写错的话，留下的是一个多余的笔记，而不是一次失败。
15. **核心 ```` ```query ```` 嵌入搜索块不出结果。** Obsidian 只在阅读模式的 `MarkdownView` 宿主下才把 ```` ```query ```` 特判成嵌入搜索；插件用的是静态 `MarkdownRenderer.render`，没有这个宿主，所以查询块会退化成普通代码块、显示查询源码而不是搜索结果。`dataview` / `base` 块不受影响（它们是正经注册的 codeblock processor）。需要动态列表时请改用 `dataview` 或 `base` 块。

## 许可

[MIT](LICENSE)
